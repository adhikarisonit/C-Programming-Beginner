#include <stdio.h>
#include <stdlib.h>

int main()
{int num,oct=0,hex=0,i,s,t;
printf("\nEnter an integer:");
scanf("%d",&num);
printf("The numbers in octal and hexadecimal are resp.:%o %x",num,num);
return 0;
}
